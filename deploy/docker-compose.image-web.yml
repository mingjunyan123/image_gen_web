services:
  image-web:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: image-web
    restart: always
    expose:
      - "80"
    networks:
      - ai-gateway

networks:
  ai-gateway:
    external: true
